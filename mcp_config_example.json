{
    "mcpServers": {
        "uv-mcp": {
            "command": "uv",
            "args": [
                "--directory",
                "${extensionPath}",
                "run",
                "uv-mcp"
            ],
            "env": {
                "PATH": "${PATH}:${HOME}/.local/bin"
            }
        }
    }
}