{
    "name": "uv-mcp",
    "version": "0.2.0",
    "description": "MCP server for uv - environment diagnostics, repair, and dependency management",
    "mcpServers": {
        "uv-mcp": {
            "command": "/home/saadman/.local/bin/uv",
            "args": [
                "--directory",
                "${extensionPath}",
                "run",
                "uv-mcp"
            ],
            "cwd": "${extensionPath}"
        }
    }
}